# Environment Variables for Teacher Bot YT

# YouTube OAuth credentials (download from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
CLIENT_SECRETS_JSON=client_secrets.json

# Token file (auto-generated on first run)
TOKEN_JSON=token.json