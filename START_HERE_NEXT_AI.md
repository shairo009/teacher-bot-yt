# START HERE — RESUME WITHOUT ASKING ROUTINE QUESTIONS

1. Read `AI_HANDOFF.md` first.
2. Read `AGENTS.md`.
3. Read `data/ai_resume_state.json`.
4. Execute the authoritative `NEXT ACTION` immediately.
5. Do NOT ask the user to re-upload missing preview history or to confirm routine offline steps.
6. If the old preview ledger is unavailable, use the pre-approved NEW isolated ledger `tmp/continuation-auto-preview` and record that it is new.
7. Update `AI_HANDOFF.md` and `data/ai_resume_state.json` after every meaningful step and before credits/context/time run out.
8. Preserve production state and all safety/uniqueness gates.
